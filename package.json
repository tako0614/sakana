{
  "name": "sakana-discord-bot",
  "version": "1.0.0",
  "description": "A simple Discord bot built with discord.js.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.js",
    "deploy": "node scripts/deploy-commands.js",
    "check": "node --check src/index.js && tsx --no-cache -e \"Promise.all([import('./src/emotion.js'), import('./src/emotion-image.jsx')])\" && node -e \"import('./src/archive/query.js').then(({ parseQuery }) => { const a = parseQuery('regex:/(a|b)/').ast; if (a.type !== 'filter' || a.value !== '/(a|b)/') throw new Error('regex paren parse broken: ' + JSON.stringify(a)); const b = parseQuery('regex:/^(?!.*http).{200,}/').ast; if (b.type !== 'filter') throw new Error('regex lookahead parse broken'); const c = parseQuery('http://x.com').ast; if (c.type !== 'term' || c.value !== 'http://x.com') throw new Error('url must stay a plain term'); const d = parseQuery('sort:new'); if (d.ast !== null || d.directives.sort !== 'new') throw new Error('sort directive broken'); console.log('query parser ok'); })\" && ARCHIVE_DB_PATH=/tmp/sakana-check.sqlite node -e \"import('./src/commands.js').then(({ commandData }) => console.log(commandData.length + ' commands built'))\" && ARCHIVE_DB_PATH=/tmp/sakana-check.sqlite DEEPSEEK_API_KEY=check node -e \"import('./src/agent/index.js').then(({ isAgentRequest }) => console.log('agent module ok: ' + typeof isAgentRequest))\" && ARCHIVE_DB_PATH=/tmp/sakana-check.sqlite node -e \"import('./src/agent/tools.js').then(({ pickSearchSource }) => { const at = (a, ok = true) => pickSearchSource(a, ok).source; for (const q of ['regex:/(a|b)/', 'a OR b', 'a -b', 'reactions:>5', 'len:>200', 'sort:long a']) if (at({ query: q }) !== 'archive') throw new Error('must route to archive: ' + q); for (const a of [{ query: 'a' }, { query: 'a', sort: 'Relevance' }, { query: 'a', after: '7d' }, { query: 'a', has: 'image' }, { query: ':(' }]) if (at(a) !== 'discord') throw new Error('must route to discord: ' + JSON.stringify(a)); if (at({ query: 'a', sort: 'Reactions' }) !== 'archive') throw new Error('sort must be case-insensitive'); if (at({ query: 'a', has: 'reaction' }) !== 'archive') throw new Error('has:reaction is archive only'); if (at({ after: '2024-05' }) !== 'archive') throw new Error('date-only search needs the archive'); if (at({ query: 'regex:/a/' }, false) !== 'unsupported') throw new Error('must refuse dsl without an index'); if (at({ query: 'a', source: 'ARCHIVE' }, false) !== 'unavailable') throw new Error('explicit archive without an index'); console.log('search routing ok'); })\" && ARCHIVE_DB_PATH=/tmp/sakana-check.sqlite node -e \"Promise.all([import('./src/archive/db.js'), import('./src/archive/query.js'), import('./src/archive/search.js')]).then(([{ msToSnowflake, snowflakeToMs }, { parseDateRange }, { buildSearch }]) => { if (snowflakeToMs(msToSnowflake(1712345678901)) !== 1712345678901) throw new Error('snowflake roundtrip broken'); if (msToSnowflake(1000000000000) !== '0') throw new Error('pre-epoch must clamp to 0'); const y = parseDateRange('2020'); if (snowflakeToMs(msToSnowflake(y.start)) !== y.start) throw new Error('after bound must be the range start'); if (snowflakeToMs(String(BigInt(msToSnowflake(y.start)) - 1n)) !== y.start - 1) throw new Error('before bound must exclude the boundary ms'); const nums = buildSearch({ guildId: '1', query: 'after:2020 before:2021' }).params.filter((p) => typeof p === 'number'); if (nums.length !== 2 || !(nums[0] < nums[1])) throw new Error('after:2020 before:2021 must not be an empty range: ' + JSON.stringify(nums)); console.log('date bounds ok'); })\" && node -e \"import('./src/agent/urlguard.js').then(async ({ inspectUrl }) => { const block = ['http://169.254.169.254/', 'http://[::ffff:7f00:1]:9222/json', 'http://[fe80::1]/', 'http://[fc00::1]/', 'http://100.64.0.1/', 'http://127.0.0.1:9222/json', 'http://192.168.0.1/', 'http://10.0.0.1/', 'http://172.20.0.1/', 'http://0/', 'http://2130706433/', 'file:///etc/passwd', 'data:text/html,x', 'chrome://version', 'view-source:https://example.com', 'javascript:alert(1)', 'http://localhost/', 'http://[64:ff9b::7f00:1]/', 'about:config']; for (const url of block) { const r = await inspectUrl(url); if (r.ok) throw new Error('SSRF guard let this through: ' + url); } for (const url of ['https://example.com/x', 'http://1.1.1.1/', 'about:blank', 'http://172.32.0.1/']) { const r = await inspectUrl(url); if (!r.ok) throw new Error('SSRF guard blocked a public target: ' + url + ' -> ' + r.reason); } console.log('url guard ok'); })\" && node -e \"import('./src/agent/sentence.js').then(({ looksLikeSentence }) => { const yes = ['レビュー無しでマージするのは絶対にやめるべき','レビュー飛ばして通した','消費税増税は必要','型安全は大事だと思う','なんで急に方針変わったの？','あの件どうなった','テストは後で書く','サーバー移行の話をしたい','should we merge without review']; const no = ['レビュー','消費税増税','型安全','たこ','デプロイ 失敗','マージ OR リベース','regex:/(a|b)/','https://example.com/x','github.com','PR レビュー 品質','サーバー移行の話','CI','2024-05']; for (const t of yes) if (!looksLikeSentence(t)) throw new Error('must look like a sentence: ' + t); for (const t of no) if (looksLikeSentence(t)) throw new Error('must look like keywords: ' + t); console.log('sentence heuristic ok (' + (yes.length + no.length) + ' cases)'); })\" && ARCHIVE_DB_PATH=/tmp/sakana-check.sqlite node -e \"Promise.all([import('./src/agent/tools.js'), import('./src/agent/browser.js')]).then(([t, b]) => { const split = t.splitMeaningQuery('from:@a レビュー要らない'); if (split.lifted.length !== 1 || split.lifted[0].key !== 'from') throw new Error('from: must lift out of a meaning query'); if (/from:/.test(split.text)) throw new Error('lifted filters must not stay in the prose'); const under = t.splitMeaningQuery('(from:@a OR from:@b) レビュー'); if (under.lifted.length !== 0) throw new Error('nothing may be lifted from under OR'); if (under.dropped.length !== 2) throw new Error('filters under OR must be reported as dropped'); const plain = t.splitMeaningQuery('レビュー無しでマージ'); if (plain.text !== 'レビュー無しでマージ' || plain.lifted.length) throw new Error('plain prose must pass through unchanged'); const refs = { resolve: (v) => (v === '3' ? { messageId: '111111111111111111', channelId: 'c', createdAt: 1 } : null) }; const anchor = (at) => t.resolveAnchor({ refs }, at).kind; if (anchor('3') !== 'message') throw new Error('a live ref number must resolve'); if (anchor('9999') !== 'error') throw new Error('a dead ref number must not become a date'); if (anchor('2020') !== 'time') throw new Error('a 4-digit year must be a date'); if (anchor('2024-05-01') !== 'time') throw new Error('a date must be a date'); if (anchor('') !== 'none') throw new Error('no anchor means latest'); const defs = (flags, full) => [t.searchToolDefinition(flags), t.readDefinition(flags.archiveAvailable), t.channelsDefinition, b.browserToolDefinition(full)]; const full = defs({ archiveAvailable: true, semanticAvailable: true }, true); const names = full.map((d) => d.function.name).join(','); if (names !== 'search,read,channels,browser') throw new Error('unexpected tool surface: ' + names); const size = JSON.stringify(full).length; if (size > 4300) throw new Error('tool definitions grew past the ceiling: ' + size); const modes = full[0].function.parameters.properties.mode.enum.join(','); if (modes !== 'keyword,meaning,count') throw new Error('mode enum wrong: ' + modes); const bare = defs({ archiveAvailable: false, semanticAvailable: false }, false)[0].function.parameters.properties; if (bare.mode || bare.by || bare.source) throw new Error('unavailable modes must not be advertised'); const readOnly = b.browserToolDefinition(false).function.parameters.properties.action.enum; for (const banned of ['eval', 'cdp', 'click', 'type']) if (readOnly.includes(banned)) throw new Error('read-only browser must not advertise ' + banned); console.log('tool surface ok (' + size + ' chars)'); })\" && ARCHIVE_DB_PATH=/tmp/sakana-check.sqlite node -e \"import('./src/archive/chunks.js').then(({ splitIntoChunks }) => { const at = (min) => ({ created_at: min * 60000, content: 'x'.repeat(10), char_count: 10 }); const gap = splitIntoChunks([at(0), at(1), at(2), at(40), at(41)], { gapMs: 900000, maxMessages: 20, maxChars: 1200 }); if (gap.length !== 2 || gap[0].length !== 3) throw new Error('silence must split: ' + JSON.stringify(gap.map((g) => g.length))); const many = splitIntoChunks(Array.from({ length: 25 }, (_, i) => at(i * 0.1)), { gapMs: 900000, maxMessages: 20, maxChars: 100000 }); if (many.length !== 2 || many[0].length !== 20) throw new Error('message cap must split: ' + JSON.stringify(many.map((g) => g.length))); const long = splitIntoChunks(Array.from({ length: 6 }, (_, i) => ({ created_at: i * 1000, content: 'x'.repeat(300), char_count: 300 })), { gapMs: 900000, maxMessages: 20, maxChars: 1200 }); if (long.length < 2) throw new Error('char cap must split'); if (splitIntoChunks([]).length !== 0) throw new Error('no messages means no chunks'); console.log('chunker ok'); })\" && ARCHIVE_DB_PATH=/tmp/sakana-check.sqlite node scripts/check-chunk-paging.mjs && node scripts/check-limits.mjs && node --check scripts/deploy-commands.js && python3 -m py_compile scripts/analyze-emotions.py scripts/embed-texts.py"
  },
  "keywords": [
    "discord",
    "bot"
  ],
  "license": "MIT",
  "dependencies": {
    "@twind/core": "^1.1.3",
    "@twind/preset-tailwind": "^1.1.4",
    "better-sqlite3": "^12.9.0",
    "discord.js": "^14.26.3",
    "dotenv": "^16.4.7",
    "node-cron": "^4.2.1",
    "node-html-to-image": "^5.0.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "tsx": "^4.21.0"
  },
  "overrides": {
    "handlebars": "^4.7.9"
  },
  "engines": {
    "node": ">=20"
  }
}
