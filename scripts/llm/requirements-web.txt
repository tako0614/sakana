-r ../../requirements.txt

# Browser export is pinned because Optimum ONNX 0.1 targets Transformers 4.x.
# export-ft-web.py bridges the Transformers 5 rope_parameters field explicitly.
transformers==4.57.6
onnx==1.22.0
onnxruntime==1.28.0
optimum-onnx[onnxruntime]==0.1.0
